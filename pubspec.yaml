name: api_tv_challenge
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  injectable: ^1.5.0
  freezed_annotation: ^1.1.0
  dio: ^4.0.4
  shared_preferences: ^2.0.11
  flutter_bloc: ^8.0.0
  sqflite: ^2.0.1
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:

  flutter_lints: ^1.0.0
  freezed: ^1.1.0
  injectable_generator:
  pretty_dio_logger: ^1.1.1

flutter:
  uses-material-design: true
